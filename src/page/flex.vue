<template>
  <div class="flex-content">
    <!--    弹性布局  -->
    <div class="flex-item left">
      <h3>
        国家药品监督管理局12月31日消息，
      </h3>
    </div>
    <div class="flex-item center">
      <h3>
        国家药品监督管理局12月31日消息，
      </h3>
      <p>
        国家药品监督管理局12月31日消息，近日，国家药品监督管理局批准厦门万泰沧海生物技术有限公司的双价人乳头瘤病毒疫苗（大肠杆菌）（商品名：馨可宁（Cecolin））上市注册申请，该药是首家获批的国产人乳头瘤病毒疫苗，适用于9-45岁女性。

        人乳头瘤病毒（HPV
      </p>
    </div>
    <div class="flex-item right">
      <pre>ECMAScript 6 入门<br><br>作者：阮一峰<br><br>授权：署名-非商用许可证<br><br>目录<br>前言<br>ECMAScript 6简介<br>let 和 const 命令<br>变量的解构赋值<br>字符串的扩展<br>字符串的新增方法<br>正则的扩展<br>数值的扩展<br>函数的扩展<br>数组的扩展<br>对象的扩展<br>对象的新增方法<br>Symbol<br>Set 和 Map 数据结构<br>Proxy<br>Reflect<br>Promise 对象<br>Iterator 和 for...of 循环<br>Generator 函数的语法<br>Generator 函数的异步应用<br>async 函数<br>Class 的基本语法<br>Class 的继承<br>Module 的语法<br>Module 的加载实现<br>编程风格<br>读懂规格<br>异步遍历器<br>ArrayBuffer<br>最新提案<br>Decorator<br>参考链接<br>其他<br>源码<br>修订历史<br>反馈意见数组的扩展<br>扩展运算符<br>Array.from()<br>Array.of()<br>数组实例的 copyWithin()<br>数组实例的 find() 和 findIndex()<br>数组实例的 fill()<br>数组实例的 entries()，keys() 和 values()<br>数组实例的 includes()<br>数组实例的 flat()，flatMap()<br>数组的空位<br>Array.prototype.sort() 的排序稳定性<br>《ES6 实战教程》 深入学习一线大厂必备 ES6 技能。VIP 教程限时免费领取。 ⇐ 立即查看<br>扩展运算符<br>含义<br><br>扩展运算符（spread）是三个点（...）。它好比 rest 参数的逆运算，将一个数组转为用逗号分隔的参数序列。<br><br>console.log(...[1, 2, 3])<br>// 1 2 3<br><br>console.log(1, ...[2, 3, 4], 5)<br>// 1 2 3 4 5<br><br>[...document.querySelectorAll('div')]<br>// [&lt;div&gt;, &lt;div&gt;, &lt;div&gt;]<br><br><br>该运算符主要用于函数调用。<br><br>function push(array, ...items) {<br>  array.push(...items);<br>}<br><br>function add(x, y) {<br>  return x + y;<br>}<br><br>const numbers = [4, 38];<br>add(...numbers) // 42<br><br><br>上面代码中，array.push(...items)和add(...numbers)这两行，都是函数的调用，它们都使用了扩展运算符。该运算符将一个数组，变为参数序列。<br><br>扩展运算符与正常的函数参数可以结合使用，非常灵活。<br><br>function f(v, w, x, y, z) { }<br>const args = [0, 1];<br>f(-1, ...args, 2, ...[3]);<br><br><br>扩展运算符后面还可以放置表达式。<br><br>const arr = [<br>  ...(x &gt; 0 ? ['a'] : []),<br>  'b',<br>];<br><br><br>如果扩展运算符后面是一个空数组，则不产生任何效果。<br><br>[...[], 1]<br>// [1]<br><br><br>注意，只有函数调用时，扩展运算符才可以放在圆括号中，否则会报错。<br><br>(...[1, 2])<br>// Uncaught SyntaxError: Unexpected number<br><br>console.log((...[1, 2]))<br>// Uncaught SyntaxError: Unexpected number<br><br>console.log(...[1, 2])<br>// 1 2<br><br><br>上面三种情况，扩展运算符都放在圆括号里面，但是前两种情况会报错，因为扩展运算符所在的括号不是函数调用。<br><br>替代函数的 apply 方法<br><br>由于扩展运算符可以展开数组，所以不再需要apply方法，将数组转为函数的参数了。<br><br>// ES5 的写法<br>function f(x, y, z) {<br>  // ...<br>}<br>var args = [0, 1, 2];<br>f.apply(null, args);<br><br>// ES6的写法<br>function f(x, y, z) {<br>  // ...<br>}<br>let args = [0, 1, 2];<br>f(...args);<br><br><br>下面是扩展运算符取代apply方法的一个实际的例子，应用Math.max方法，简化求出一个数组最大元素的写法。<br><br>// ES5 的写法<br>Math.max.apply(null, [14, 3, 77])<br><br>// ES6 的写法<br>Math.max(...[14, 3, 77])<br><br>// 等同于<br>Math.max(14, 3, 77);<br><br><br>上面代码中，由于 JavaScript 不提供求数组最大元素的函数，所以只能套用Math.max函数，将数组转为一个参数序列，然后求最大值。有了扩展运算符以后，就可以直接用Math.max了。<br><br>另一个例子是通过push函数，将一个数组添加到另一个数组的尾部。<br><br>// ES5的 写法<br>var arr1 = [0, 1, 2];<br>var arr2 = [3, 4, 5];<br>Array.prototype.push.apply(arr1, arr2);<br><br>// ES6 的写法<br>let arr1 = [0, 1, 2];<br>let arr2 = [3, 4, 5];<br>arr1.push(...arr2);<br><br><br>上面代码的 ES5 写法中，push方法的参数不能是数组，所以只好通过apply方法变通使用push方法。有了扩展运算符，就可以直接将数组传入push方法。<br><br>下面是另外一个例子。<br><br>// ES5<br>new (Date.bind.apply(Date, [null, 2015, 1, 1]))<br>// ES6<br>new Date(...[2015, 1, 1]);<br><br>扩展运算符的应用<br><br>（1）复制数组<br><br>数组是复合的数据类型，直接复制的话，只是复制了指向底层数据结构的指针，而不是克隆一个全新的数组。<br><br>const a1 = [1, 2];<br>const a2 = a1;<br><br>a2[0] = 2;<br>a1 // [2, 2]<br><br><br>上面代码中，a2并不是a1的克隆，而是指向同一份数据的另一个指针。修改a2，会直接导致a1的变化。<br><br>ES5 只能用变通方法来复制数组。<br><br>const a1 = [1, 2];<br>const a2 = a1.concat();<br><br>a2[0] = 2;<br>a1 // [1, 2]<br><br><br>上面代码中，a1会返回原数组的克隆，再修改a2就不会对a1产生影响。<br><br>扩展运算符提供了复制数组的简便写法。<br><br>const a1 = [1, 2];<br>// 写法一<br>const a2 = [...a1];<br>// 写法二<br>const [...a2] = a1;<br><br><br>上面的两种写法，a2都是a1的克隆。<br><br>（2）合并数组<br><br>扩展运算符提供了数组合并的新写法。<br><br>const arr1 = ['a', 'b'];<br>const arr2 = ['c'];<br>const arr3 = ['d', 'e'];<br><br>// ES5 的合并数组<br>arr1.concat(arr2, arr3);<br>// [ 'a', 'b', 'c', 'd', 'e' ]<br><br>// ES6 的合并数组<br>[...arr1, ...arr2, ...arr3]<br>// [ 'a', 'b', 'c', 'd', 'e' ]<br><br><br>不过，这两种方法都是浅拷贝，使用的时候需要注意。<br><br>const a1 = [{ foo: 1 }];<br>const a2 = [{ bar: 2 }];<br><br>const a3 = a1.concat(a2);<br>const a4 = [...a1, ...a2];<br><br>a3[0] === a1[0] // true<br>a4[0] === a1[0] // true<br><br><br>上面代码中，a3和a4是用两种不同方法合并而成的新数组，但是它们的成员都是对原数组成员的引用，这就是浅拷贝。如果修改了引用指向的值，会同步反映到新数组。<br><br>（3）与解构赋值结合<br><br>扩展运算符可以与解构赋值结合起来，用于生成数组。<br><br>// ES5<br>a = list[0], rest = list.slice(1)<br>// ES6<br>[a, ...rest] = list<br><br><br>下面是另外一些例子。<br><br>const [first, ...rest] = [1, 2, 3, 4, 5];<br>first // 1<br>rest  // [2, 3, 4, 5]<br><br>const [first, ...rest] = [];<br>first // undefined<br>rest  // []<br><br>const [first, ...rest] = ["foo"];<br>first  // "foo"<br>rest   // []<br><br><br>如果将扩展运算符用于数组赋值，只能放在参数的最后一位，否则会报错。<br><br>const [...butLast, last] = [1, 2, 3, 4, 5];<br>// 报错<br><br>const [first, ...middle, last] = [1, 2, 3, 4, 5];<br>// 报错<br><br><br>（4）字符串<br><br>扩展运算符还可以将字符串转为真正的数组。<br><br>[...'hello']<br>// [ "h", "e", "l", "l", "o" ]<br><br><br>上面的写法，有一个重要的好处，那就是能够正确识别四个字节的 Unicode 字符。<br><br>'x\uD83D\uDE80y'.length // 4<br>[...'x\uD83D\uDE80y'].length // 3<br><br><br>上面代码的第一种写法，JavaScript 会将四个字节的 Unicode 字符，识别为 2 个字符，采用扩展运算符就没有这个问题。因此，正确返回字符串长度的函数，可以像下面这样写。<br><br>function length(str) {<br>  return [...str].length;<br>}<br><br>length('x\uD83D\uDE80y') // 3<br><br><br>凡是涉及到操作四个字节的 Unicode 字符的函数，都有这个问题。因此，最好都用扩展运算符改写。<br><br>let str = 'x\uD83D\uDE80y';<br><br>str.split('').reverse().join('')<br>// 'y\uDE80\uD83Dx'<br><br>[...str].reverse().join('')<br>// 'y\uD83D\uDE80x'<br><br><br>上面代码中，如果不用扩展运算符，字符串的reverse操作就不正确。<br><br>（5）实现了 Iterator 接口的对象<br><br>任何定义了遍历器（Iterator）接口的对象（参阅 Iterator 一章），都可以用扩展运算符转为真正的数组。<br><br>let nodeList = document.querySelectorAll('div');<br>let array = [...nodeList];<br><br><br>上面代码中，querySelectorAll方法返回的是一个NodeList对象。它不是数组，而是一个类似数组的对象。这时，扩展运算符可以将其转为真正的数组，原因就在于NodeList对象实现了 Iterator 。<br><br>Number.prototype[Symbol.iterator] = function*() {<br>  let i = 0;<br>  let num = this.valueOf();<br>  while (i &lt; num) {<br>    yield i++;<br>  }<br>}<br><br>console.log([...5]) // [0, 1, 2, 3, 4]<br><br><br>上面代码中，先定义了Number对象的遍历器接口，扩展运算符将5自动转成Number实例以后，就会调用这个接口，就会返回自定义的结果。<br><br>对于那些没有部署 Iterator 接口的类似数组的对象，扩展运算符就无法将其转为真正的数组。<br><br>let arrayLike = {<br>  '0': 'a',<br>  '1': 'b',<br>  '2': 'c',<br>  length: 3<br>};<br><br>// TypeError: Cannot spread non-iterable object.<br>let arr = [...arrayLike];<br><br><br>上面代码中，arrayLike是一个类似数组的对象，但是没有部署 Iterator 接口，扩展运算符就会报错。这时，可以改为使用Array.from方法将arrayLike转为真正的数组。<br><br>（6）Map 和 Set 结构，Generator 函数<br><br>扩展运算符内部调用的是数据结构的 Iterator 接口，因此只要具有 Iterator 接口的对象，都可以使用扩展运算符，比如 Map 结构。<br><br>let map = new Map([<br>  [1, 'one'],<br>  [2, 'two'],<br>  [3, 'three'],<br>]);<br><br>let arr = [...map.keys()]; // [1, 2, 3]<br><br><br>Generator 函数运行后，返回一个遍历器对象，因此也可以使用扩展运算符。<br><br>const go = function*(){<br>  yield 1;<br>  yield 2;<br>  yield 3;<br>};<br><br>[...go()] // [1, 2, 3]<br><br><br>上面代码中，变量go是一个 Generator 函数，执行后返回的是一个遍历器对象，对这个遍历器对象执行扩展运算符，就会将内部遍历得到的值，转为一个数组。<br><br>如果对没有 Iterator 接口的对象，使用扩展运算符，将会报错。<br><br>const obj = {a: 1, b: 2};<br>let arr = [...obj]; // TypeError: Cannot spread non-iterable object<br><br>Array.from()<br><br>Array.from方法用于将两类对象转为真正的数组：类似数组的对象（array-like object）和可遍历（iterable）的对象（包括 ES6 新增的数据结构 Set 和 Map）。<br><br>下面是一个类似数组的对象，Array.from将它转为真正的数组。<br><br>let arrayLike = {<br>    '0': 'a',<br>    '1': 'b',<br>    '2': 'c',<br>    length: 3<br>};<br><br>// ES5的写法<br>var arr1 = [].slice.call(arrayLike); // ['a', 'b', 'c']<br><br>// ES6的写法<br>let arr2 = Array.from(arrayLike); // ['a', 'b', 'c']<br><br><br>实际应用中，常见的类似数组的对象是 DOM 操作返回的 NodeList 集合，以及函数内部的arguments对象。Array.from都可以将它们转为真正的数组。<br><br>// NodeList对象<br>let ps = document.querySelectorAll('p');<br>Array.from(ps).filter(p =&gt; {<br>  return p.textContent.length &gt; 100;<br>});<br><br>// arguments对象<br>function foo() {<br>  var args = Array.from(arguments);<br>  // ...<br>}<br><br><br>上面代码中，querySelectorAll方法返回的是一个类似数组的对象，可以将这个对象转为真正的数组，再使用filter方法。<br><br>只要是部署了 Iterator 接口的数据结构，Array.from都能将其转为数组。<br><br>Array.from('hello')<br>// ['h', 'e', 'l', 'l', 'o']<br><br>let namesSet = new Set(['a', 'b'])<br>Array.from(namesSet) // ['a', 'b']<br><br><br>上面代码中，字符串和 Set 结构都具有 Iterator 接口，因此可以被Array.from转为真正的数组。<br><br>如果参数是一个真正的数组，Array.from会返回一个一模一样的新数组。<br><br>Array.from([1, 2, 3])<br>// [1, 2, 3]<br><br><br>值得提醒的是，扩展运算符（...）也可以将某些数据结构转为数组。<br><br>// arguments对象<br>function foo() {<br>  const args = [...arguments];<br>}<br><br>// NodeList对象<br>[...document.querySelectorAll('div')]<br><br><br>扩展运算符背后调用的是遍历器接口（Symbol.iterator），如果一个对象没有部署这个接口，就无法转换。Array.from方法还支持类似数组的对象。所谓类似数组的对象，本质特征只有一点，即必须有length属性。因此，任何有length属性的对象，都可以通过Array.from方法转为数组，而此时扩展运算符就无法转换。<br><br>Array.from({ length: 3 });<br>// [ undefined, undefined, undefined ]<br><br><br>上面代码中，Array.from返回了一个具有三个成员的数组，每个位置的值都是undefined。扩展运算符转换不了这个对象。<br><br>对于还没有部署该方法的浏览器，可以用Array.prototype.slice方法替代。<br><br>const toArray = (() =&gt;<br>  Array.from ? Array.from : obj =&gt; [].slice.call(obj)<br>)();<br><br><br>Array.from还可以接受第二个参数，作用类似于数组的map方法，用来对每个元素进行处理，将处理后的值放入返回的数组。<br><br>Array.from(arrayLike, x =&gt; x * x);<br>// 等同于<br>Array.from(arrayLike).map(x =&gt; x * x);<br><br>Array.from([1, 2, 3], (x) =&gt; x * x)<br>// [1, 4, 9]<br><br><br>下面的例子是取出一组 DOM 节点的文本内容。<br><br>let spans = document.querySelectorAll('span.name');<br><br>// map()<br>let names1 = Array.prototype.map.call(spans, s =&gt; s.textContent);<br><br>// Array.from()<br>let names2 = Array.from(spans, s =&gt; s.textContent)<br><br><br>下面的例子将数组中布尔值为false的成员转为0。<br><br>Array.from([1, , 2, , 3], (n) =&gt; n || 0)<br>// [1, 0, 2, 0, 3]<br><br><br>另一个例子是返回各种数据的类型。<br><br>function typesOf () {<br>  return Array.from(arguments, value =&gt; typeof value)<br>}<br>typesOf(null, [], NaN)<br>// ['object', 'object', 'number']<br><br><br>如果map函数里面用到了this关键字，还可以传入Array.from的第三个参数，用来绑定this。<br><br>Array.from()可以将各种值转为真正的数组，并且还提供map功能。这实际上意味着，只要有一个原始的数据结构，你就可以先对它的值进行处理，然后转成规范的数组结构，进而就可以使用数量众多的数组方法。<br><br>Array.from({ length: 2 }, () =&gt; 'jack')<br>// ['jack', 'jack']<br><br><br>上面代码中，Array.from的第一个参数指定了第二个参数运行的次数。这种特性可以让该方法的用法变得非常灵活。<br><br>Array.from()的另一个应用是，将字符串转为数组，然后返回字符串的长度。因为它能正确处理各种 Unicode 字符，可以避免 JavaScript 将大于\uFFFF的 Unicode 字符，算作两个字符的 bug。<br><br>function countSymbols(string) {<br>  return Array.from(string).length;<br>}<br><br>Array.of()<br><br>Array.of方法用于将一组值，转换为数组。<br><br>Array.of(3, 11, 8) // [3,11,8]<br>Array.of(3) // [3]<br>Array.of(3).length // 1<br><br><br>这个方法的主要目的，是弥补数组构造函数Array()的不足。因为参数个数的不同，会导致Array()的行为有差异。<br><br>Array() // []<br>Array(3) // [, , ,]<br>Array(3, 11, 8) // [3, 11, 8]<br><br><br>上面代码中，Array方法没有参数、一个参数、三个参数时，返回结果都不一样。只有当参数个数不少于 2 个时，Array()才会返回由参数组成的新数组。参数个数只有一个时，实际上是指定数组的长度。<br><br>Array.of基本上可以用来替代Array()或new Array()，并且不存在由于参数不同而导致的重载。它的行为非常统一。<br><br>Array.of() // []<br>Array.of(undefined) // [undefined]<br>Array.of(1) // [1]<br>Array.of(1, 2) // [1, 2]<br><br><br>Array.of总是返回参数值组成的数组。如果没有参数，就返回一个空数组。<br><br>Array.of方法可以用下面的代码模拟实现。<br><br>function ArrayOf(){<br>  return [].slice.call(arguments);<br>}<br><br>数组实例的 copyWithin()<br><br>数组实例的copyWithin()方法，在当前数组内部，将指定位置的成员复制到其他位置（会覆盖原有成员），然后返回当前数组。也就是说，使用这个方法，会修改当前数组。<br><br>Array.prototype.copyWithin(target, start = 0, end = this.length)<br><br><br>它接受三个参数。<br><br>target（必需）：从该位置开始替换数据。如果为负值，表示倒数。<br>start（可选）：从该位置开始读取数据，默认为 0。如果为负值，表示从末尾开始计算。<br>end（可选）：到该位置前停止读取数据，默认等于数组长度。如果为负值，表示从末尾开始计算。<br><br>这三个参数都应该是数值，如果不是，会自动转为数值。<br><br>[1, 2, 3, 4, 5].copyWithin(0, 3)<br>// [4, 5, 3, 4, 5]<br><br><br>上面代码表示将从 3 号位直到数组结束的成员（4 和 5），复制到从 0 号位开始的位置，结果覆盖了原来的 1 和 2。<br><br>下面是更多例子。<br><br>// 将3号位复制到0号位<br>[1, 2, 3, 4, 5].copyWithin(0, 3, 4)<br>// [4, 2, 3, 4, 5]<br><br>// -2相当于3号位，-1相当于4号位<br>[1, 2, 3, 4, 5].copyWithin(0, -2, -1)<br>// [4, 2, 3, 4, 5]<br><br>// 将3号位复制到0号位<br>[].copyWithin.call({length: 5, 3: 1}, 0, 3)<br>// {0: 1, 3: 1, length: 5}<br><br>// 将2号位到数组结束，复制到0号位<br>let i32a = new Int32Array([1, 2, 3, 4, 5]);<br>i32a.copyWithin(0, 2);<br>// Int32Array [3, 4, 5, 4, 5]<br><br>// 对于没有部署 TypedArray 的 copyWithin 方法的平台<br>// 需要采用下面的写法<br>[].copyWithin.call(new Int32Array([1, 2, 3, 4, 5]), 0, 3, 4);<br>// Int32Array [4, 2, 3, 4, 5]<br><br>数组实例的 find() 和 findIndex()<br><br>数组实例的find方法，用于找出第一个符合条件的数组成员。它的参数是一个回调函数，所有数组成员依次执行该回调函数，直到找出第一个返回值为true的成员，然后返回该成员。如果没有符合条件的成员，则返回undefined。<br><br>[1, 4, -5, 10].find((n) =&gt; n &lt; 0)<br>// -5<br><br><br>上面代码找出数组中第一个小于 0 的成员。<br><br>[1, 5, 10, 15].find(function(value, index, arr) {<br>  return value &gt; 9;<br>}) // 10<br><br><br>上面代码中，find方法的回调函数可以接受三个参数，依次为当前的值、当前的位置和原数组。<br><br>数组实例的findIndex方法的用法与find方法非常类似，返回第一个符合条件的数组成员的位置，如果所有成员都不符合条件，则返回-1。<br><br>[1, 5, 10, 15].findIndex(function(value, index, arr) {<br>  return value &gt; 9;<br>}) // 2<br><br><br>这两个方法都可以接受第二个参数，用来绑定回调函数的this对象。<br><br>function f(v){<br>  return v &gt; this.age;<br>}<br>let person = {name: 'John', age: 20};<br>[10, 12, 26, 15].find(f, person);    // 26<br><br><br>上面的代码中，find函数接收了第二个参数person对象，回调函数中的this对象指向person对象。<br><br>另外，这两个方法都可以发现NaN，弥补了数组的indexOf方法的不足。<br><br>[NaN].indexOf(NaN)<br>// -1<br><br>[NaN].findIndex(y =&gt; Object.is(NaN, y))<br>// 0<br><br><br>上面代码中，indexOf方法无法识别数组的NaN成员，但是findIndex方法可以借助Object.is方法做到。<br><br>数组实例的 fill()<br><br>fill方法使用给定值，填充一个数组。<br><br>['a', 'b', 'c'].fill(7)<br>// [7, 7, 7]<br><br>new Array(3).fill(7)<br>// [7, 7, 7]<br><br><br>上面代码表明，fill方法用于空数组的初始化非常方便。数组中已有的元素，会被全部抹去。<br><br>fill方法还可以接受第二个和第三个参数，用于指定填充的起始位置和结束位置。<br><br>['a', 'b', 'c'].fill(7, 1, 2)<br>// ['a', 7, 'c']<br><br><br>上面代码表示，fill方法从 1 号位开始，向原数组填充 7，到 2 号位之前结束。<br><br>注意，如果填充的类型为对象，那么被赋值的是同一个内存地址的对象，而不是深拷贝对象。<br><br>let arr = new Array(3).fill({name: "Mike"});<br>arr[0].name = "Ben";<br>arr<br>// [{name: "Ben"}, {name: "Ben"}, {name: "Ben"}]<br><br>let arr = new Array(3).fill([]);<br>arr[0].push(5);<br>arr<br>// [[5], [5], [5]]<br><br>数组实例的 entries()，keys() 和 values()<br><br>ES6 提供三个新的方法——entries()，keys()和values()——用于遍历数组。它们都返回一个遍历器对象（详见《Iterator》一章），可以用for...of循环进行遍历，唯一的区别是keys()是对键名的遍历、values()是对键值的遍历，entries()是对键值对的遍历。<br><br>for (let index of ['a', 'b'].keys()) {<br>  console.log(index);<br>}<br>// 0<br>// 1<br><br>for (let elem of ['a', 'b'].values()) {<br>  console.log(elem);<br>}<br>// 'a'<br>// 'b'<br><br>for (let [index, elem] of ['a', 'b'].entries()) {<br>  console.log(index, elem);<br>}<br>// 0 "a"<br>// 1 "b"<br><br><br>如果不使用for...of循环，可以手动调用遍历器对象的next方法，进行遍历。<br><br>let letter = ['a', 'b', 'c'];<br>let entries = letter.entries();<br>console.log(entries.next().value); // [0, 'a']<br>console.log(entries.next().value); // [1, 'b']<br>console.log(entries.next().value); // [2, 'c']<br><br>数组实例的 includes()<br><br>Array.prototype.includes方法返回一个布尔值，表示某个数组是否包含给定的值，与字符串的includes方法类似。ES2016 引入了该方法。<br><br>[1, 2, 3].includes(2)     // true<br>[1, 2, 3].includes(4)     // false<br>[1, 2, NaN].includes(NaN) // true<br><br><br>该方法的第二个参数表示搜索的起始位置，默认为0。如果第二个参数为负数，则表示倒数的位置，如果这时它大于数组长度（比如第二个参数为-4，但数组长度为3），则会重置为从0开始。<br><br>[1, 2, 3].includes(3, 3);  // false<br>[1, 2, 3].includes(3, -1); // true<br><br><br>没有该方法之前，我们通常使用数组的indexOf方法，检查是否包含某个值。<br><br>if (arr.indexOf(el) !== -1) {<br>  // ...<br>}<br><br><br>indexOf方法有两个缺点，一是不够语义化，它的含义是找到参数值的第一个出现位置，所以要去比较是否不等于-1，表达起来不够直观。二是，它内部使用严格相等运算符（===）进行判断，这会导致对NaN的误判。<br><br>[NaN].indexOf(NaN)<br>// -1<br><br><br>includes使用的是不一样的判断算法，就没有这个问题。<br><br>[NaN].includes(NaN)<br>// true<br><br><br>下面代码用来检查当前环境是否支持该方法，如果不支持，部署一个简易的替代版本。<br><br>const contains = (() =&gt;<br>  Array.prototype.includes<br>    ? (arr, value) =&gt; arr.includes(value)<br>    : (arr, value) =&gt; arr.some(el =&gt; el === value)<br>)();<br>contains(['foo', 'bar'], 'baz'); // =&gt; false<br><br><br>另外，Map 和 Set 数据结构有一个has方法，需要注意与includes区分。<br><br>Map 结构的has方法，是用来查找键名的，比如Map.prototype.has(key)、WeakMap.prototype.has(key)、Reflect.has(target, propertyKey)。<br>Set 结构的has方法，是用来查找值的，比如Set.prototype.has(value)、WeakSet.prototype.has(value)。<br>数组实例的 flat()，flatMap()<br><br>数组的成员有时还是数组，Array.prototype.flat()用于将嵌套的数组“拉平”，变成一维的数组。该方法返回一个新数组，对原数据没有影响。<br><br>[1, 2, [3, 4]].flat()<br>// [1, 2, 3, 4]<br><br><br>上面代码中，原数组的成员里面有一个数组，flat()方法将子数组的成员取出来，添加在原来的位置。<br><br>flat()默认只会“拉平”一层，如果想要“拉平”多层的嵌套数组，可以将flat()方法的参数写成一个整数，表示想要拉平的层数，默认为1。<br><br>[1, 2, [3, [4, 5]]].flat()<br>// [1, 2, 3, [4, 5]]<br><br>[1, 2, [3, [4, 5]]].flat(2)<br>// [1, 2, 3, 4, 5]<br><br><br>上面代码中，flat()的参数为2，表示要“拉平”两层的嵌套数组。<br><br>如果不管有多少层嵌套，都要转成一维数组，可以用Infinity关键字作为参数。<br><br>[1, [2, [3]]].flat(Infinity)<br>// [1, 2, 3]<br><br><br>如果原数组有空位，flat()方法会跳过空位。<br><br>[1, 2, , 4, 5].flat()<br>// [1, 2, 4, 5]<br><br><br>flatMap()方法对原数组的每个成员执行一个函数（相当于执行Array.prototype.map()），然后对返回值组成的数组执行flat()方法。该方法返回一个新数组，不改变原数组。<br><br>// 相当于 [[2, 4], [3, 6], [4, 8]].flat()<br>[2, 3, 4].flatMap((x) =&gt; [x, x * 2])<br>// [2, 4, 3, 6, 4, 8]<br><br><br>flatMap()只能展开一层数组。<br><br>// 相当于 [[[2]], [[4]], [[6]], [[8]]].flat()<br>[1, 2, 3, 4].flatMap(x =&gt; [[x * 2]])<br>// [[2], [4], [6], [8]]<br><br><br>上面代码中，遍历函数返回的是一个双层的数组，但是默认只能展开一层，因此flatMap()返回的还是一个嵌套数组。<br><br>flatMap()方法的参数是一个遍历函数，该函数可以接受三个参数，分别是当前数组成员、当前数组成员的位置（从零开始）、原数组。<br><br>arr.flatMap(function callback(currentValue[, index[, array]]) {<br>  // ...<br>}[, thisArg])<br><br><br>flatMap()方法还可以有第二个参数，用来绑定遍历函数里面的this。<br><br>数组的空位<br><br>数组的空位指，数组的某一个位置没有任何值。比如，Array构造函数返回的数组都是空位。<br><br>Array(3) // [, , ,]<br><br><br>上面代码中，Array(3)返回一个具有 3 个空位的数组。<br><br>注意，空位不是undefined，一个位置的值等于undefined，依然是有值的。空位是没有任何值，in运算符可以说明这一点。<br><br>0 in [undefined, undefined, undefined] // true<br>0 in [, , ,] // false<br><br><br>上面代码说明，第一个数组的 0 号位置是有值的，第二个数组的 0 号位置没有值。<br><br>ES5 对空位的处理，已经很不一致了，大多数情况下会忽略空位。<br><br>forEach(), filter(), reduce(), every() 和some()都会跳过空位。<br>map()会跳过空位，但会保留这个值<br>join()和toString()会将空位视为undefined，而undefined和null会被处理成空字符串。<br>// forEach方法<br>[,'a'].forEach((x,i) =&gt; console.log(i)); // 1<br><br>// filter方法<br>['a',,'b'].filter(x =&gt; true) // ['a','b']<br><br>// every方法<br>[,'a'].every(x =&gt; x==='a') // true<br><br>// reduce方法<br>[1,,2].reduce((x,y) =&gt; x+y) // 3<br><br>// some方法<br>[,'a'].some(x =&gt; x !== 'a') // false<br><br>// map方法<br>[,'a'].map(x =&gt; 1) // [,1]<br><br>// join方法<br>[,'a',undefined,null].join('#') // "#a##"<br><br>// toString方法<br>[,'a',undefined,null].toString() // ",a,,"<br><br><br>ES6 则是明确将空位转为undefined。<br><br>Array.from方法会将数组的空位，转为undefined，也就是说，这个方法不会忽略空位。<br><br>Array.from(['a',,'b'])<br>// [ "a", undefined, "b" ]<br><br><br>扩展运算符（...）也会将空位转为undefined。<br><br>[...['a',,'b']]<br>// [ "a", undefined, "b" ]<br><br><br>copyWithin()会连空位一起拷贝。<br><br>[,'a','b',,].copyWithin(2,0) // [,"a",,"a"]<br><br><br>fill()会将空位视为正常的数组位置。<br><br>new Array(3).fill('a') // ["a","a","a"]<br><br><br>for...of循环也会遍历空位。<br><br>let arr = [, ,];<br>for (let i of arr) {<br>  console.log(1);<br>}<br>// 1<br>// 1<br><br><br>上面代码中，数组arr有两个空位，for...of并没有忽略它们。如果改成map方法遍历，空位是会跳过的。<br><br>entries()、keys()、values()、find()和findIndex()会将空位处理成undefined。<br><br>// entries()<br>[...[,'a'].entries()] // [[0,undefined], [1,"a"]]<br><br>// keys()<br>[...[,'a'].keys()] // [0,1]<br><br>// values()<br>[...[,'a'].values()] // [undefined,"a"]<br><br>// find()<br>[,'a'].find(x =&gt; true) // undefined<br><br>// findIndex()<br>[,'a'].findIndex(x =&gt; true) // 0<br><br><br>由于空位的处理规则非常不统一，所以建议避免出现空位。<br><br>Array.prototype.sort() 的排序稳定性<br><br>排序稳定性（stable sorting）是排序算法的重要属性，指的是排序关键字相同的项目，排序前后的顺序不变。<br><br>const arr = [<br>  'peach',<br>  'straw',<br>  'apple',<br>  'spork'<br>];<br><br>const stableSorting = (s1, s2) =&gt; {<br>  if (s1[0] &lt; s2[0]) return -1;<br>  return 1;<br>};<br><br>arr.sort(stableSorting)<br>// ["apple", "peach", "straw", "spork"]<br><br><br>上面代码对数组arr按照首字母进行排序。排序结果中，straw在spork的前面，跟原始顺序一致，所以排序算法stableSorting是稳定排序。<br><br>const unstableSorting = (s1, s2) =&gt; {<br>  if (s1[0] &lt;= s2[0]) return -1;<br>  return 1;<br>};<br><br>arr.sort(unstableSorting)<br>// ["apple", "peach", "spork", "straw"]<br><br><br>上面代码中，排序结果是spork在straw前面，跟原始顺序相反，所以排序算法unstableSorting是不稳定的。<br><br>常见的排序算法之中，插入排序、合并排序、冒泡排序等都是稳定的，堆排序、快速排序等是不稳定的。不稳定排序的主要缺点是，多重排序时可能会产生问题。假设有一个姓和名的列表，要求按照“姓氏为主要关键字，名字为次要关键字”进行排序。开发者可能会先按名字排序，再按姓氏进行排序。如果排序算法是稳定的，这样就可以达到“先姓氏，后名字”的排序效果。如果是不稳定的，就不行。<br><br>早先的 ECMAScript 没有规定，Array.prototype.sort()的默认排序算法是否稳定，留给浏览器自己决定，这导致某些实现是不稳定的。ES2019 明确规定，Array.prototype.sort()的默认排序算法必须稳定。这个规定已经做到了，现在 JavaScript 各个主要实现的默认排序算法都是稳定的。<br><br>留言《ES6 实战教程》 深入学习一线大厂必备 ES6 技能。VIP 教程限时免费领取。 ⇐ 立即查看back to topeditLoading ...Oops! ... File not found!上一章下一章上一章下一章</pre>
    </div>
  </div>
</template>

<script>
export default {
  name: 'flex'
}
</script>

<style lang="scss" scoped>
  .flex-content{
    display: flex;
    flex-wrap: wrap;
    background-color: aliceblue;
    justify-content: space-around;
    align-content: center;
    .flex-item{
      background-color: aquamarine;
      color: #fff;
      padding: 20px;
      margin: 5px;
      height: auto;
      overflow: auto;
    }
    .left {
      background-color: #746474;
      align-self: flex-end;
    }
    .center {
      width: 800px;
      background-color: #2c3e50;
    }
    .right {
      background-color: black;
    }
  }
</style>
